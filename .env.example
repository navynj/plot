# Neon dev-branch connection string (Postgres). Used by the app (Next.js loads
# .env) and by drizzle-kit migrate (drizzle.config.ts loads it via dotenv).
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
